# Registry sync files

Generated by `replicateEverything::prepare_study_for_registry()`.

- Copy `replication.yml` to `registry/studies/10.1371_journal.pone.0278337.yml`
- Merge `index.csv` into the registry repo `index.csv` (replace any existing row with the same DOI).

Or run `replicateEverything::sync_study_to_registry()` with `registry_root` pointing at your registry checkout.
