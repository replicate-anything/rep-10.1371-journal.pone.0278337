# Load a precomputed display artifact from `inst/report/artifacts/`

Load a precomputed display artifact from `inst/report/artifacts/`

## Usage

``` r
load_artifact(id)
```

## Arguments

- id:

  Replication id.

## Value

For figures, a path to a PNG; for tables, an HTML string; or `NULL`.
