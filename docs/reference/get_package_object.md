# Load a dataset or object from this package

LazyData objects are not visible in the package namespace; use `::`
access.

## Usage

``` r
get_package_object(name, package = pkg_name())
```

## Arguments

- name:

  Object name.

- package:

  Package name.

## Value

The requested object.
