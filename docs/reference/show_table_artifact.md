# Show a precomputed table artifact in the vignette

Show a precomputed table artifact in the vignette

## Usage

``` r
show_table_artifact(id)
```

## Arguments

- id:

  Replication id (e.g. `"tab_1"`).

## Value

A
[`knitr::asis_output()`](https://rdrr.io/pkg/knitr/man/asis_output.html)
object.
