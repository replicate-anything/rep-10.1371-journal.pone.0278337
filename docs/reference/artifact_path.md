# Path to a baked artifact file under `inst/report/artifacts/`

Path to a baked artifact file under `inst/report/artifacts/`

## Usage

``` r
artifact_path(id, ext)
```

## Arguments

- id:

  Replication id.

- ext:

  File extension (`png`, `html`, `rds`, ...).
